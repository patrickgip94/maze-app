/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  images: {
    domains: ['i.imgur.com']
  }
}

// In order for next.js to add images you must include the domain url.